import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-manager',
  webDir: '../../dist/apps/ionic-manager',
  bundledWebRuntime: false,
};

export default config;
